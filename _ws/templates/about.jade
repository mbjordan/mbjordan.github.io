extends layout

block head
    meta(name='description' content='About Matt Jordan - honyovk')
    link(rel="canonical" href="#{locals.url}#{page.url}")

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}

block header
  include author
  h2= page.title

block content
  article.article
    section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href=contents.index.url) « Full blog
